# Core Dynamixel SDK
dynamixel-sdk>=3.7.31

# MuJoCo simulator
mujoco>=3.1.0

# GPIO — Jetson Orin Nano
# Install: pip install Jetson.GPIO
# Also add user to gpio group: sudo usermod -aG gpio $USER
Jetson.GPIO>=2.1.0

# ROS 2 Python packages (installed via apt / rosdep, listed for reference)
# rclpy
# sensor_msgs
# std_msgs

# Utilities
numpy>=1.24.0
